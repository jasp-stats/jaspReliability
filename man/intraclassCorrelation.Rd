% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intraclassCorrelationWrapper.R
\name{intraclassCorrelation}
\alias{intraclassCorrelation}
\title{Intraclass Correlation}
\usage{
intraclassCorrelation(
  data = NULL,
  version = "0.97.1",
  averagedRating = FALSE,
  ci = TRUE,
  ciLevel = 0.95,
  iccType = "icc1",
  plotHeight = 320,
  plotWidth = 480,
  variables = list(types = list(), value = list())
)
}
\arguments{
\item{averagedRating, }{When ratings are averaged across raters, the k-rater version of the selected ICC is computed (e.g. ICC(2,k) instead of ICC(2,1)) per Shrout & Fleiss (1979). Averaging generally raises the reliability estimate.
Defaults to \code{FALSE}.}

\item{ci, }{Report a confidence interval for the ICC estimate based on the F-distribution.
Defaults to \code{TRUE}.}

\item{iccType, }{Determines the ICC model. ICC(1): each subject rated by a different random rater. ICC(2): all subjects rated by the same random sample of raters. ICC(3): all subjects rated by the same fixed raters. See Shrout & Fleiss (1979).
\itemize{
\item \code{"icc3"}
\item \code{"icc1"}
\item \code{"icc2"}
}}

\item{variables, }{Rating variables to include. Each variable is one rater; each row is a subject being rated.}
}
\description{
Intraclass Correlation
}
