% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardErrorOfMeasurementWrapper.R
\name{standardErrorOfMeasurement}
\alias{standardErrorOfMeasurement}
\title{Standard Error of Measurement}
\usage{
standardErrorOfMeasurement(
  data = NULL,
  version = "0.97.1",
  anova = FALSE,
  ciLevelPlots = 0.95,
  ciLevelTable = 0.95,
  combinedPointPlot = FALSE,
  feldt = FALSE,
  feldtNumberOfSplits = "2",
  hideTable = FALSE,
  histogramCounts = FALSE,
  irt = FALSE,
  keats = FALSE,
  lord = FALSE,
  lord2 = FALSE,
  lord2NumberOfSplits = "2",
  minimumGroupSize = 20,
  mollenkopfFeldt = FALSE,
  mollenkopfFeldtNumberOfSplits = "2",
  mollenkopfFeldtPolyDegree = 2,
  plotHeight = 320,
  plotWidth = 480,
  pointPlots = FALSE,
  reliabilityValue = 0.5,
  sumScoreCiPlots = FALSE,
  sumScoreCiPlotsCutoff = FALSE,
  sumScoreCiPlotsCutoffValue = 0,
  sumScoreCiTable = FALSE,
  thorndike = FALSE,
  userReliability = FALSE,
  variables = list(types = list(), value = list())
)
}
\arguments{
\item{anova, }{Estimates conditional SEM from a repeated-measures ANOVA using the ICC(3,k) approach (Emons, 2023).
Defaults to \code{FALSE}.}

\item{combinedPointPlot, }{Combined plot displaying all methods' SEM values in a single panel for easy comparison.
Defaults to \code{FALSE}.}

\item{feldt, }{Splits items into multiple equal parts to estimate error variance per score group.
Defaults to \code{FALSE}.}

\item{feldtNumberOfSplits, }{How many equal parts to split the test into. Must be a divisor of the number of items.}

\item{hideTable, }{Hide the main SEM table showing conditional error estimates per sum score and method.
Defaults to \code{FALSE}.}

\item{histogramCounts, }{Histogram of the number of respondents per sum score group.
Defaults to \code{FALSE}.}

\item{irt, }{IRT-based SEM using the 2PLM for dichotomous items or the graded response model for polytomous items. Assumes a single latent variable.
Defaults to \code{FALSE}.}

\item{keats, }{Keats' correction of Lord's method; uses a reliability coefficient to reduce bias.
Defaults to \code{FALSE}.}

\item{lord, }{Lord's binomial method based on the number of correct and incorrect responses.
Defaults to \code{FALSE}.}

\item{lord2, }{Lord's method generalised to multiple test parts.
Defaults to \code{FALSE}.}

\item{lord2NumberOfSplits, }{How many parts to split the test into. Must be a divisor of the number of items (excluding the last value).}

\item{minimumGroupSize, }{Score groups with fewer observations are merged with adjacent groups before estimating the SEM (default: 20). Applies to the Thorndike, Feldt, ANOVA, and Lord generalized methods.}

\item{mollenkopfFeldt, }{Splits items into parts and models score differences with polynomial regression to estimate the conditional SEM.
Defaults to \code{FALSE}.}

\item{mollenkopfFeldtNumberOfSplits, }{How many equal parts to split the test into. Must be a divisor of the number of items.}

\item{mollenkopfFeldtPolyDegree, }{Degree of the polynomial used to predict score differences, e.g., 3 fits Y = X + X² + X³.}

\item{pointPlots, }{Separate point plot per method showing the conditional SEM values across sum scores.
Defaults to \code{FALSE}.}

\item{sumScoreCiPlots, }{Plot sum scores with confidence interval bands per method. Intervals are normal-theory CIs using the estimated SEM.
Defaults to \code{FALSE}.}

\item{sumScoreCiPlotsCutoff, }{Add a horizontal reference line at the specified cut score.
Defaults to \code{FALSE}.}

\item{sumScoreCiTable, }{Table of sum scores with normal-theory confidence intervals computed from the estimated SEM.
Defaults to \code{FALSE}.}

\item{thorndike, }{Splits items into odd- and even-numbered halves. Reorder variables in the list to change the split.
Defaults to \code{FALSE}.}

\item{userReliability, }{Override the reliability estimate used for the unconditional SEM and the Keats method.
Defaults to \code{FALSE}.}

\item{variables, }{Items to include in the SEM analysis. Must be ordinally or nominally scaled (dichotomous or polytomous).}
}
\description{
Standard Error of Measurement
}
